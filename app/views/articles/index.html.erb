<!DOCTYPE html>
<html>
<title>Blogs Wall</title>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1">
<body>

    <div class="jumbotron text-center p-2 my-5">
        <h1 class="display-4"> <b> Articles </b> </h1>
    </div>

    <div class="container-fluid">
        <div class="row">
            <% @articles.each do |article| %>
            
            <div class="col-4">
                <div class="position-relative">
                <div class="title-topleft"> <%= article.tag %> </div>
                
                <a href="/articles/<%= article.id %>">
                    <%= image_tag article.image_link, style: "width:100%", alt: 'Blog-image'  %>
                </a>
                <p class="mt-2"> <%= article.title %> </p>
                </div>
            </div>
            <% end %>

        </div>
            
        <div class="flickr_pagination d-flex justify-content-center">
            <%= will_paginate @posts %>
        </div>
    </div>


</body>
</html>
