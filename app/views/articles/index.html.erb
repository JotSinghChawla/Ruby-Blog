<!DOCTYPE html>
<html>
<title>Blogs Wall</title>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1">
<body>

    <div class="jumbotron text-center p-2 mt-4 mb-5">
        <h1 class="display-4"> <b> Articles </b> </h1>
    </div>

    <div class="container-fluid">
        <div class="row">
            <% @articles.each do |article| %>
            
            <div class="col-4">
                <div class="position-relative my-4">
                <div class="title-topleft"> <%= article.tag %> </div>
                
                <a href="/articles/<%= article.id %>">
                    <%= image_tag article.image_link, style: "width:100%; height: 300px", alt: 'Blog-image'  %>
                </a>
                <div class="mt-2 d-flex mb-5 pb-3 bellow_post"> 
                    <p class="h2 my-auto "> <%= article.title %> </p>
                    <span class="my-auto blockquote-footer font-italic ml-auto" style="font-size: 1rem">
                     by <%= article.user.username.capitalize %> 
                    </span>
                </div>
                </div>
            </div>
            <% end %>

        </div>
            
        <div class="flickr_pagination d-flex justify-content-center">
            <%= will_paginate @posts %>
        </div>
    </div>


</body>
</html>
